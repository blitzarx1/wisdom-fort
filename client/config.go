package client

// Config is the configuration for the client
type Config struct {
	// ServerHost is the port of the server
	ServerPort uint

	// ServerHost is the host of the server
	ServerHost string
}
